export enum UserAction {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
  VIEW = 'VIEW',
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
}

export enum UserTypeEnum {
  STAFF = 'STAFF',
  CLIENT = 'CLIENT',
  SUPERUSER = 'SUPERUSER',
  ADMIN = 'ADMIN',
}
